import EmptyState from '../../components/ui/EmptyState.jsx';

function NotFoundPage() {
  return <EmptyState title="Pagina no encontrada" message="La ruta solicitada no existe." />;
}

export default NotFoundPage;
